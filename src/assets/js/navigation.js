/* Navigation (hamburger accessible, clavier, aria) — à compléter en T08 */
export function initNavigation() {
  // TODO T08 : toggle menu, aria-expanded, fermeture à la touche Échap,
  // focus visible, respect prefers-reduced-motion.
}